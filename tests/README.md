# Website tests

`test_site.py` builds the MkDocs site, serves the built output locally, and uses Playwright to verify that core pages render without browser-side failures.

Run the full local website review with:

```bash
bash scripts/review_site.sh
```
